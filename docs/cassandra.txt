Cassandra数据库具有扩展性、高可用性且不需要妥协（牺牲）性能。在商用硬件或云基础设施上，线性扩展和被证明的容错性使它成为关键数据的完
美平台。对跨数据中心复制数据的支持是同类中做的最好的，提供低延时，可对付区域性中断。

被证明，很多大公司用。容错性，数据自动在多节点间复制，失败节点可以无需停机进行替换。性能好，一贯地比其它可选的流行NoSQL产品性能要好，
主要因为基础架构选择的好。分散式存储，不存在单点失败，不存在网络瓶颈，集群中的每个节点完全一样。扩展性，75000个节点，存储10PB数据，
每天处理1T个请求。持久性，适合于即使整个数据中心宕掉也不允许数据丢失的应用。自己掌控，可以选择同步或异步复制，异步操作高可用采用提示
移交和读修复特性进行了优化。弹性，通过增加新机器实现读写操作的线性增长，且无需停机或中断应用。